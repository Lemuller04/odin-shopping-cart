import styles from "./Footer.module.css";

const Footer = () => {
    return (
        <footer className={styles["main"]}>
            <p>
                &copy; Lemü
            </p>
        </footer>
    );
};

export default Footer;
